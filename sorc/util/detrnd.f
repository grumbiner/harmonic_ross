      SUBROUTINE detrnd(x, y, n, bnot, bwon, avg, go_ahead)
! Robert Grumbine

      IMPLICIT none
      LOGICAL go_ahead
      REAL bnot, bwon, avg
      INTEGER n
      REAL x(n), y(n)
      
      RETURN
      END 
